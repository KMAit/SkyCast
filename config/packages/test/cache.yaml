framework:
  cache:
    app: cache.adapter.array
    system: cache.adapter.array
    default_redis_provider: null

    # ✅ Override specific pools to use array cache in test too
    pools:
      cache.weather:
        adapter: cache.adapter.array
        provider: null
        default_lifetime: 0
