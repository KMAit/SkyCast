# Environment for PHPUnit and functional tests
APP_ENV=test
APP_DEBUG=1

# Kernel and security
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'

# Database — isolated, fast, no external dependency
DATABASE_URL="sqlite:///%kernel.project_dir%/var/test.db"

# Cache — in-memory only (no Redis)
CACHE_DSN=cache.adapter.array

REDIS_URL="redis://localhost"
